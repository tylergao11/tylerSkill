# Templates

Copyable workflow templates live here.

Templates should be short, UTF-8, and stable. If a template becomes specific to a
project, copy it into the relevant `docs/project-notes/`, `docs/handoffs/`,
`docs/reviews/`, or `docs/decisions/` folder instead of editing the shared
template.
