# Tools

Larger reusable tools and future packages live here.

Use `scripts/` for small single-file CLIs. Move a script into `tools/<name>/`
when it needs multiple files, dependencies, packaging, richer documentation, or
its own release lifecycle.
