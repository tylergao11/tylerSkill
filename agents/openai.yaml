interface:
  display_name: "Agent Collaboration OS"
  short_description: "Multi-agent game production workflow"
  default_prompt: "Use $agent-collaboration-os to coordinate a game feature from planning through specialist handoffs, testing, and release confidence."

policy:
  allow_implicit_invocation: true
